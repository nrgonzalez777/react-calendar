export { default as appointmentEditorTypes } from './types';
export { default as appointmentEditorSelectors } from './selectors';
export { default as appointmentEditorActions } from './actions';

import reducer from './reducer';

export default reducer;
